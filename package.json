{
  "name": "cohab-registro",
  "version": "1.0.0",
  "description": "Sistema de Registro COHAB - Frontend + MongoDB API",
  "main": "mongodb-api/server.js",
  "scripts": {
    "build": "cd mongodb-api && npm install",
    "start": "cd mongodb-api && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "cors": "^2.8.5"
  }
}

